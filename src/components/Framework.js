import React, { Component } from 'react';

export class Framework extends Component { // eslint-disable-line
  render() {
    return (
      <div>
        framework
      </div>
    );
  }
}

export default Framework;

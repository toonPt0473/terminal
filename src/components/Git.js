import React, { Component } from 'react';

export class Git extends Component { // eslint-disable-line
  render() {
    return (
      <div>
        git
      </div>
    );
  }
}

export default Git;

import React, { Component } from 'react';

export class Work extends Component { // eslint-disable-line
  render() {
    return (
      <div>
        work
      </div>
    );
  }
}

export default Work;
